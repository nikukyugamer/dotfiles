#!/bin/bash
set -euo pipefail
# cd "$(dirname "$0")"

echo 'Ruby version:'
~/.rbenv/shims/ruby -v

~/.rbenv/shims/bundle config set --local path 'vendor/bundle'
~/.rbenv/shims/bundle install && ~/.rbenv/shims/gem install solargraph rubocop rubocop-rails rubocop-rspec

exit 0

